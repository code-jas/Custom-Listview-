<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#EDE6E8E6"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/anime_title"
        android:layout_width="305dp"
        android:layout_height="93dp"
        android:layout_marginTop="4dp"
        android:fontFamily="@font/poppins_bold"
        android:text="@string/demon_slayer_kimetsu_no_yaiba"
        android:textAlignment="center"
        android:textColor="@color/font_color"
        android:textSize="30sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/character_title"
        android:layout_width="305dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/poppins_bold"
        android:text="@string/character_details"
        android:textAlignment="center"
        android:textColor="@color/font_color"
        android:textSize="17sp"
        app:layout_constraintEnd_toEndOf="@+id/anime_title"
        app:layout_constraintStart_toStartOf="@+id/anime_title"
        app:layout_constraintTop_toBottomOf="@+id/anime_title" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/demonslayer_recycler_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:paddingBottom="150dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/character_title"
        app:layout_constraintVertical_bias="0.112" />

</androidx.constraintlayout.widget.ConstraintLayout>